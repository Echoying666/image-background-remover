name = "bg-remover"
main = "functions/api/_middleware.js"
compatibility_date = "2024-01-01"

[vars]
# 可以在这里添加环境变量，但敏感信息应该通过 wrangler secret 配置

# Remove.bg API Key 需要通过以下命令配置:
# wrangler secret put REMOVE_BG_API_KEY
# 然后输入你的 Remove.bg API Key
